% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MRBEE_LDA.R
\name{MRBEE_LDA}
\alias{MRBEE_LDA}
\title{MRBEE_LDA for LD-aware Multivariable Mendelian Randomization}
\usage{
MRBEE_LDA(
  by,
  bX,
  byse,
  bXse,
  LD = "identity",
  Rxy,
  cluster.index = c(1:length(by)),
  use.susie = T,
  inference = "subsampling",
  epsilon = 0.5,
  standardize = F,
  group.penalize = F,
  group.index = c(1:ncol(bX)[1]),
  group.diff = 100,
  tauvec = seq(4, 8, by = 0.5),
  admm.rho = 2,
  Lvec = c(1:min(10, ncol(bX))),
  pip.thres = 0.25,
  estimate_residual_variance = T,
  max.iter = 50,
  max.eps = 1e-05,
  susie.iter = 100,
  ebic.theta = 0,
  ebic.gamma = 1,
  ridge.diff = 1000,
  estimate_residual_method = "MoM",
  sampling.time = 300,
  sampling.iter = 25,
  group.size = 4,
  maxdiff = 3,
  reliability.thres = 0.5,
  coverage.causal = 0.95,
  theta.ini = F,
  gamma.ini = F,
  verbose = T,
  projection.eigen.floor = 1
)
}
\arguments{
\item{by}{A vector of effect estimates from the outcome GWAS.}

\item{bX}{A matrix of effect estimates from the exposure GWAS.}

\item{byse}{A vector of standard errors of effect estimates from the outcome GWAS.}

\item{bXse}{A matrix of standard errors of effect estimates from the exposure GWAS.}

\item{LD}{The linkage disequilibrium (LD) matrix. Default is the identity matrix, assuming independent instrumental variables (IVs).}

\item{Rxy}{The correlation matrix of estimation errors of exposures and outcome GWAS. The last column corresponds to the outcome.}

\item{cluster.index}{A vector indicating the LD block indices each IV belongs to. The length is equal to the number of IVs, and values are the LD block indices.}

\item{use.susie}{An indicator of whether using SuSiE to select causal exposures. Defaults to \code{T}.}

\item{inference}{Inference method when \code{use.susie = TRUE}. \code{"subsampling"} (default) uses grouped LD subsampling; \code{"datafission"} uses one Gaussian selection view and an independent fixed-support oracle inference view.}

\item{epsilon}{Information fraction allocated to the data-fission selection view. Must be between \code{0.1} and \code{0.9}, inclusive. Default is \code{0.5}.}

\item{standardize}{Whether SuSiE should standardize its sufficient statistics again. MRBEE_LDA constructs unit-variance exposure and outcome inputs before fitting, so the default is \code{FALSE}.}

\item{group.penalize}{An indicator of whether using difference penalty to penalize highly correlated exposures. Defaults to \code{F}.}

\item{group.index}{A vector of the group index of exposure. Defaults to \code{c(1:ncol(bX))}.}

\item{group.diff}{The tuning penalizing difference of highly correlated exposure prediction. Defaults to \code{100}.}

\item{tauvec}{The candidate vector of tuning parameters for the MCP penalty function. Default is \code{seq(4, 8, by=0.5)}.}

\item{admm.rho}{The tuning parameter in the nested ADMM algorithm. Default is \code{2}.}

\item{Lvec}{When SuSiE is used, the candidate vector for the number of single effects. Default is \code{c(1:min(10, ncol(bX)))}.}

\item{pip.thres}{Posterior inclusion probability (PIP) threshold. Individual PIPs less than this value will be shrunk to zero. Default is \code{0.25}.}

\item{estimate_residual_variance}{When SuSiE is used, an indicator of whether estimating the variance of residuals. If setting F, the variance of residual will be fixed as \code{Rxy[p+1,p+1]}. Default is \code{T}.}

\item{max.iter}{Maximum number of iterations for causal effect estimation. Defaults to \code{50}.}

\item{max.eps}{Tolerance for stopping criteria. Defaults to \code{1e-5}.}

\item{susie.iter}{Number of iterations in SuSiE per iteration. Default is \code{100}.}

\item{ebic.theta}{EBIC factor on causal effect. Default is \code{0}.}

\item{ebic.gamma}{EBIC factor on horizontal pleiotropy. Default is \code{1}.}

\item{ridge.diff}{A ridge.parameter on the differences of causal effect estimate in one credible set. Defaults to \code{1e3}.}

\item{estimate_residual_method}{The method used for estimating residual variance. For the original SuSiE model, "MLE" and "MoM" estimation is equivalent, but for the infinitesimal model, "MoM" is more stable.}

\item{sampling.time}{Number of fixed half-SNP sampling repeats when \code{use.susie = TRUE}. This is not used when \code{use.susie = FALSE}. Default is \code{300}.}

\item{sampling.iter}{Number of estimation iterations per sampling repeat when \code{use.susie = TRUE}. This is not used when \code{use.susie = FALSE}. Default is \code{25}.}

\item{group.size}{Target size for block-aware subsampling groups when \code{use.susie = TRUE}. Singleton LD blocks are sampled independently with probability 0.5, and size-two LD blocks are sampled intact. Larger blocks are randomly reordered between blocks while preserving their internal IV order, concatenated, divided into groups of this size, and half of the groups are sampled. Default is \code{4}.}

\item{maxdiff}{The maximum difference between the MRBEE causal estimate and the initial estimator. Defaults to \code{3}.}

\item{reliability.thres}{A threshold for the minimum value of the reliability ratio. If the original reliability ratio is less than this threshold, only part of the estimation error is removed so that the working reliability ratio equals this threshold. Default is 0.5.}

\item{coverage.causal}{The coverage of defining a credible set when \code{use.susie = T}. Defaults to \code{0.95}.}

\item{theta.ini}{Initial value of theta. If \code{FALSE}, the default method is used to estimate. Default is \code{FALSE}.}

\item{gamma.ini}{Initial value of gamma. Default is \code{FALSE}.}

\item{verbose}{A logical indicator of whether to display the execution time of the method. Default is \code{T}.}

\item{projection.eigen.floor}{The minimum eigenvalue used when projecting SuSiE and selected refit cross-product matrices. The full-data floor is this value; resampled matrices are scaled by their current row count divided by the full row count. Defaults to \code{1}.}
}
\value{
A list that contains the results of MRBEE_LDA:
\describe{
  \item{\code{theta}}{Causal effect estimate.}
  \item{\code{theta.se}}{Standard error of the causal effect estimate.}
  \item{\code{theta.cov}}{Covariance matrix of the causal effect estimate.}
  \item{\code{theta.pip}}{Empirical posterior inclusion probability (PIP) of the causal effect when \code{use.susie = TRUE}.}
  \item{\code{gamma}}{Estimate of horizontal pleiotropy.}
  \item{\code{Bic}}{A vector or matrix recording the Bayesian Information Criterion (BIC) values.}
  \item{\code{theta.ini}}{Initial value of \code{theta} used in the estimation procedure.}
  \item{\code{gamma.ini}}{Initial value of \code{gamma} used in the estimation procedure.}
  \item{\code{reliability.adjust}}{A global vector of covariance scaling factors.}
  \item{\code{thetalist}}{List of \code{theta} estimates recorded during the sampling procedure when available.}
  \item{\code{gammalist}}{List of \code{gamma} estimates recorded during the sampling procedure when available.}
}
}
\description{
MRBEE_LDA is the LD-aware multivariable MR component in the MRBEEX package. It removes weak instrument bias using bias-corrected estimating equations and identifies uncorrelated horizontal pleiotropy (UHP) using the IPOD algorithm, where outliers are interpreted as UHP. When \code{use.susie = FALSE}, the function returns the direct IPOD result without resampling. When \code{use.susie = TRUE}, it integrates SuSiE for exposure selection and uses the newer fixed half-SNP sampling procedure for inference.
}
